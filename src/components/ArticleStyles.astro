<style>
.article-page {
  padding: 44px 0;
  background: #fff;
}

.article-container {
  max-width: 860px;
  margin: 0 auto;
  font-size: 1.15rem;
  line-height: 1.8;
}

.page-kicker {
  color: #d99a00;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: .08em;
  font-size: .85rem;
  margin-bottom: 10px;
}

.article-container h1 {
  font-size: clamp(2.2rem, 5vw, 4rem);
  line-height: 1.05;
  margin-bottom: 18px;
  color: #0b2f4a;
}

.article-dek {
  font-size: 1.35rem;
  color: #475569;
  margin-bottom: 30px;
  line-height: 1.5;
}

.article-container h2 {
  margin-top: 42px;
  margin-bottom: 16px;
  color: #0b2f4a;
  border-top: 3px solid #f0b323;
  padding-top: 12px;
}

.article-container h3 {
  margin-top: 24px;
  margin-bottom: 10px;
  color: #12344d;
}

.article-container p {
  margin-bottom: 18px;
}

.article-container ul,
.article-container ol {
  margin: 18px 0;
  padding-left: 24px;
}

.article-container li {
  margin-bottom: 10px;
}

.article-container .check-list {
  list-style: none;
  padding-left: 0;
}

.article-container .check-list li::before {
  content: "✅ ";
}

.article-container .question-list {
  list-style: none;
  padding-left: 0;
}

.article-container .question-list li::before {
  content: "❓ ";
}

.info-box,
.callout,
.stat-box,
.mini-card {
  background: #f8fbff;
  border: 1px solid #dbeafe;
  border-radius: 16px;
  padding: 22px;
  margin: 24px 0;
}

.info-box {
  border-left: 5px solid #f0b323;
}

.callout {
  background: #0b2f4a;
  color: white;
  border: none;
  font-weight: 700;
}

.stat-box {
  background: linear-gradient(135deg,#0b2f4a,#0e7490);
  color: white;
  text-align: center;
}

.stat-box strong {
  display: block;
  color: #f0b323;
  margin-bottom: 8px;
}

.stat-box span {
  display: block;
  font-size: 2rem;
  font-weight: 900;
}

.two-column {
  display: grid;
  grid-template-columns: repeat(2,1fr);
  gap: 20px;
  margin: 24px 0;
}

blockquote {
  border-left: 5px solid #f0b323;
  background: #fff7e6;
  padding: 18px;
  margin: 24px 0;
  font-size: 1.2rem;
  font-weight: 700;
}

@media (max-width: 768px) {
  .article-container {
    font-size: 1.05rem;
  }

  .two-column {
    grid-template-columns: 1fr;
  }

  .article-container h1 {
    font-size: 2.2rem;
  }

  .article-dek {
    font-size: 1.15rem;
  }
}
</style>
